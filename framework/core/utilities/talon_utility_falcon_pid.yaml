name: talon_utility_falcon_pid
description: Utility for constructing Falcon process ID links from aid and TargetProcessId.
visualization:
  options:
    columns:
    - fieldName: '@timestamp'
      format: datetime
      type: field
      width: 200
    - groupByPrefix: false
      header: Field List
      type: fieldList
    newestAtBottom: true
    showOnlyFirstLine: false
  type: list-view
labels:
- talon_hunt_framework
- asset
- utility
- falconpid
- global
$schema: https://schemas.humio.com/query/v0.6.0
timeInterval:
  isLive: false
  start: 15m
queryString: |-
  // =====================================================
  // UTILITY
  // =====================================================
  | falconPID := coalesce([ContextProcessId, RpcClientProcessId, WritingProcessId])
