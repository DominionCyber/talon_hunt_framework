name: talon_win_deconflict_unsignedmoduleload
description: Aliases fields to UnsignedModule-prefixed names for safe joins. Maps FileName, FilePath, SHA256HashData, and related attributes.
visualization:
  options:
    columns:
    - fieldName: '@timestamp'
      format: datetime
      type: field
      width: 200
    - groupByPrefix: false
      header: Field List
      type: fieldList
    newestAtBottom: true
    showOnlyFirstLine: false
  type: list-view
labels:
- talon_hunt_framework
- asset
- deconflict
- unsignedmoduleload
- windows
$schema: https://schemas.humio.com/query/v0.6.0
timeInterval:
  isLive: false
  start: 15m
queryString: |-
  // =====================================================
  // DECONFLICT FIELDS
  // =====================================================
  | UnsignedModuleFileName := FileName
  | UnsignedModuleFilePath := FilePath
  | UnsignedModuleSHA256HashData := SHA256HashData
